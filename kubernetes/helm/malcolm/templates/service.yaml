apiVersion: v1
kind: Service
metadata:
  name: {{ include "malcolm.fullname" . }}
  labels:
    {{- include "malcolm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "malcolm.selectorLabels" . | nindent 4 }}
